apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-service
  labels:
    app: content-service
    keel.sh/policy: all
    keel.sh/trigger: poll
  annotations:
      keel.sh/pollSchedule: "@every 10s"
      keel.sh/policy: "glob:ui-content-microservice*"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: content-service
  template:
    metadata:
      labels:
        app: content-service
    spec:
      containers:
      - name: content-service
        image: aimvector/marcel-dev:ui-content-microservice-0.0.4
        ports:
        - containerPort: 80
---
kind: Service
apiVersion: v1
metadata:
  name: content-service
spec:
  selector:
    app: content-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80