apiVersion: apps/v1
kind: Deployment
metadata:
  name: footer-service
  labels:
    app: footer-service
    keel.sh/policy: force
    keel.sh/trigger: poll
  annotations:
      keel.sh/pollSchedule: "@every 1m"
      keel.sh/policy: major"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: footer-service
  template:
    metadata:
      labels:
        app: footer-service
    spec:
      containers:
      - name: footer-service
        image: aimvector/marcel-dev:0.0.5-uifooterservice
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: myregistrykey
---
kind: Service
apiVersion: v1
metadata:
  name: footer-service
spec:
  selector:
    app: footer-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80